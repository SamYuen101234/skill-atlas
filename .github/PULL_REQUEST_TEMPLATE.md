## What does this change?

## Why

## Checklist

- [ ] `npm test` passes
- [ ] No new runtime dependency added (or the reason for one is explained above)
- [ ] If a `scanner.js` detection pattern changed, a case was added to `test/scanner.test.js`
- [ ] If this changes what the scanner finds, the README's "What it finds" section is still accurate
